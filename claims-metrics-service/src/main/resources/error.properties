# ============================================================
# error.properties — Centralised validation error messages
# Messages are looked up by key: {AnnotationName.objectName.fieldName}
# ============================================================

# --- ClaimKpiRequestDTO ---
NotBlank.claimKpiRequestDTO.claimId=claimId is required
NotBlank.claimKpiRequestDTO.metricName=metricName is required
NotNull.claimKpiRequestDTO.metricValue=metricValue is required
NotNull.claimKpiRequestDTO.metricDate=metricDate is required
Size.claimKpiRequestDTO.claimId=claimId must be between 3 and 100 characters
Size.claimKpiRequestDTO.metricName=metricName must not exceed 50 characters
Pattern.claimKpiRequestDTO.claimId=claimId must contain only letters, digits, hyphens or underscores
Pattern.claimKpiRequestDTO.metricName=metricName must contain only letters, digits, underscores or spaces
DecimalMin.claimKpiRequestDTO.metricValue=metricValue must be zero or greater
